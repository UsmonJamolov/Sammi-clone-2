type CourseType = {
	slug: string;
	title: string;
	thumbnail: string;
};

type SourceCodeType = {
	_id: string;
	title: string;
	repositoryUrl: string;
};

type UserType = {
	email: string;
	firstName: string;
	lastName: string;
};